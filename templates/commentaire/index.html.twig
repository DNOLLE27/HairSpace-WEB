{% extends 'base.html.twig' %}

{% block title %}HairSpace - Avis{% endblock %}

{% block body %}


<link href="../assets/style.css" rel="stylesheet"/>

<div class="example-wrapper">
    <h1 class="align">Commentaire</h1>
    <div class="boxCommentaire">
           <div class="leNom"> 
            {% if app.session.get('Droit') == 1 %}
            {{ app.session.get('ID') }}   
            {% else %}
            {% if app.session.get('Droit') == 0 %}
            {{ app.session.get('ID') }}
            {% else %}Nom Prenom
            {% endif %}
            {% endif %} : 
            </div>
     {{ form_start(ajoutAvis) }}
    {{ form_widget(ajoutAvis.avs_commentaire) }}</div>
    <button type="submit" class="btnConnect">Ajouter commentaire</button></td>
    {{ form_end(ajoutAvis) }}
    </div>

</div>

<script type="text/javascript" src="assets/main.js"></script>

{% endblock %}
